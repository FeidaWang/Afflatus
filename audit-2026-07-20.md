# 🛠️ 项目审计与持续改进路线图（2026-07-20）

> 审计方式：全量实测（typecheck / vitest / vite build 均实跑）+ 逐 pillar 代码核查 + 对照 tech.md / design.md 已裁决条目。所有断言均有代码或运行证据，未沿用文档口径未经核实的说法（tech.md §9-19 纪律）。

## 1. 执行摘要

工程健康度**很高**：`tsc --noEmit` 零错误；vitest **57 文件 / 717 条全绿**（4.7s）；`vite build` 成功且全部 chunk 在 CI 体积预算内（main 191.85KB / 250KB、vendor-three 674.55KB / 700KB、astronomy 46.31KB / 60KB）；CI 五件套（测试/类型/schema/`!important` 守门/体积预算）真实存在并挂在 push/PR 上。纯函数层测试纪律、防坑台账、SSOT 文档体系是同类项目里罕见的资产。

当前最大的问题**不在功能，而在仓库熵**，三个阻塞项：

1. **构建产物入库**：28 个 `dist_*_check` 目录共 **73MB、1,776 个文件被 git 跟踪**（.gitignore 只挡了 `dist` 和 `dist_old_*`）。另有 `ladder_smoke*.mjs`、`sweep_v17c.mjs`、`outsidetest`（空文件）等临时脚本已入库，工作区还有 `probe_delete_test.txt`、`arena-a-open-2026-07-17.json` 两个未跟踪残留（后者正是 §9-16 点名「永不 add」的那类文件）。
2. **styles.css 分层名存实亡**：8,064 行中 `@layer legacy` 占第 17–7,984 行（约 99%），tokens/components/overrides 三层合计约 80 行；`!important` 存量 **2,504 处**。CI 挡住了增量，但存量迁移（A2 Phase 5）尚未开始。
3. **首页 raw-GL 渲染器无 context-restored 重建**：`saturnRenderer` / blackhole 只有 `webglcontextlost` preventDefault，`grep contextrestored src/main.js` 为空——GPU 重置后黑屏直到刷新（B6 已立项未做，本次代码级确认仍开放）。

## 2. Pillar 1：图形与渲染

* **🔴 紧急问题：**
    * 上述 raw-GL context-restored 缺失（main.js:1556/1679 只 preventDefault，不重建）。对照组做对了：sectorsStarfield.js:190、topdownCombat.js:86、alphardForge.js:165 均有 restored 回调。
* **🟡 优化机会：**
    * **DPR 上限三套并存**（B6 确认）：cap=2（main.js 2D、alphardForge、sectorsGraphView、terminalStarMap）、1.75（topdownCombat:552、capitalShip3D:252）、1.5 移动（sectorsStarfield:481）。backgroundScene.js 的 `BUDGET_PX=3.6M` 绝对像素预算法是全站最优解，建议推广为统一 DPR 策略而不是各处魔数。
    * rAF/可见性门控不均匀：IntersectionObserver 门控 6 处、`document.hidden` 6 处，但集合不重合（如 shipHologram、topdownCombat 未见 IO 门控）——按 §5.3 rAF 纪律逐场景补齐。
    * 跃迁点 shader 弱机自适应（B6 原条目，未动）。
* **🟢 长期增强：**
    * C3：three WebGPURenderer + Bloom/ACES 评估——维持「真机基线先行」门槛（U22b），不提前。
    * boot.html U29 P4 后处理栈（色差/颗粒/暗角/冲击波）——依赖 U29 裁决，见 Pillar 3。

## 3. Pillar 2：3D 建模与资产

* **🔴 紧急问题：**
    * 无。资产层非常克制：`public/assets` 全部 444KB（最大单文件 f47 图集 104KB），字体自托管 woff2、小说数据已分片（564KB）。
* **🟡 优化机会：**
    * 程序化船体（odinHull/carrierHull/wedgeCruiserHull）纯函数化 + 比例单测的打法很好，且 `add()` 回调让同一几何喂 PBR 与全息两种消费端——新增舰型时沿用此模板，并保持「涂黑看剪影」验收（宪章④）。
    * course.html 单文件 179KB（gzip 55.9KB）是全站最重 HTML 入口，长期可考虑内容外置 JSON（与 novels 同模式），非紧急。
* **🟢 长期增强：**
    * B3：combatHudSC 机库跑道纵深透视（已记录在案，需接起降路径，勿单独立项——维持原裁决）。
    * tools/sprite-baker 管线文档化，避免图集再生成知识只在会话里。

## 4. Pillar 3：战斗机制与算法

* **🔴 紧急问题：**
    * 无。纯逻辑层（weaponClock/flightPath/cameraMath/weaponCameraDirector + bootengine 8 个测试文件）全部有测试且已核实接线（weaponClock 调用方：main.js、topdownCombat.js、flightPath.js——§9-5「完成≠接入」已不复现）。src 内 TODO/FIXME 为零。
* **🟡 优化机会：**
    * **双引擎栈并存**：`src/combat`（生产）与 `src/bootengine`（2,218 行 TS，boot.html 原型专用）。当前隔离干净（src/lib 仅注释引用、无 import），但 U23/U29 裁决拖得越久，6DOF/simCore 与生产战斗层的分叉成本越高——建议把「bootengine 去留」并入 U23 裁决一并拍板。
    * 残余独立计时器：main.js:1837 `barrage=setInterval(…,78)` 是高频计时器挂在主循环外（§9-11 精神范围内，建议折进主循环）；halley 30s / voyageIndicator 60s / league 1s 低频可容忍。
    * V19 Phase 2/3（predCalibration 校准 + 信号卡 UI）：维持「攒够 60 交易日真实数据」触发条件，勿提前。
* **🟢 长期增强：**
    * U27 战术连线/星云 flag（`?combatview=topdown&tacticalines=1|nebula=1`）真机试看后裁决转默认/保留/删除。
    * U29 P3 演出相机（滞后 G 力 + Catmull-Rom 镜头轨 + 演出优先评分器）。

## 5. Pillar 4：UI/UX（Web 与移动端）

* **🔴 紧急问题：**
    * 无用户可见破版（2026-07-18 九页审计后唯一破版 `.macro` 已修，本次未发现新增）。styles.css 存量熵（摘要第 2 条）是维护性而非用户侧紧急问题，列入行动计划。
* **🟡 优化机会：**
    * `:focus-visible` 在 styles.css 仅 6 处——对照 design.md「一切可点元素 hover/active/focus-visible 三态」验收条，首页大概率有欠账，建议做一次键盘走查（Tab 遍历首页全部可点元素）。
    * 断点动物园：860/880/980/1180/1260px 并存（860 为主、18 处）。迁移 @layer 时顺路收敛到 2–3 个标准断点，不单独立项。
    * 首页「先英后中」闪变：已裁决留给 serial-Astro 评估，不重复立项，仅提示勿遗忘。
    * `<head>` 样板 ~250 行 × 9 页：维持「构建期脚本合并」的机会主义定位。
* **🟢 长期增强：**
    * `.rotateHint` 横屏提示组件：维持「有真实落点才上线」。
    * termGlossary 按页扩充（宁缺毋滥）；Tier 0 零基建召回环（分享卡/ICS/streak 文案）。
    * A2 Phase 4 剩余（main.js 3,552 行中 state/nav/boot 拆分）——真机会话执行，沙盒不强做。

## 6. 🚀 优先行动计划（后续 3 步）

1. **仓库大扫除（半天，零风险，收益立现）**：`git rm -r --cached dist_*_check`（1,776 文件 / 73MB）+ 移除 `ladder_smoke*.mjs`、`sweep_v17c.mjs`、`outsidetest`；.gitignore 补 `dist_*`；删除 `probe_delete_test.txt`，处置 `arena-a-open-2026-07-17.json`（§9-16）。顺带执行 games.html 归档（世界杯今日收官，按 tech.md §2.1 计划转档至 stats）。
2. **B6 WebGL 收尾（1 次会话）**：为 saturnRenderer/blackhole 补 `webglcontextrestored` 重建（照抄 sectorsStarfield/topdownCombat 现成模式）；同一 PR 内以 backgroundScene 的 BUDGET_PX 法统一各场景 DPR 策略（每场景按相机距离范围重推常量，§9-10）。真机验收关闭。
3. **styles.css @layer 存量迁移启动（切片制）**：按 U21 Phase 1 先例小切片推进——每片迁移一个组件族出 legacy 层、消一批 `!important` 并记录计数下降（2,504 → 目标逐季递减），CI 基线随迁移收紧。与断点收敛（860 系合并）同 PR 顺路做。

---
*交叉引用：本文件为一次性审计快照；活跃待办总账仍以 tech.md §10 为唯一真源，上述新增项（仓库大扫除）建议并入该表。*
